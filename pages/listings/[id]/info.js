import ListingInfo from '../../../components/ListingInfo';
import Link from 'next/link';

const Info = ({ listing }) => {
    return (
        <>
            <ListingInfo listing={listing} />
     
        </>
    );
};

export default Info;
